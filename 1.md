# 仮想環境の作成と起動
python3 -m venv venv
source venv/bin/activate  # Windowsなら venv\Scripts\activate

# 必須ライブラリのインストール
pip install langchain langchain-community langchain-openai fastapi uvicorn chromadb pypdf
pip install langchain-google-genai sentence-transformers
### 【コード解説】AIに資料を読み込ませる「心臓部」
以下は、PDFを読み込んでベクトルDBに保存するまでの「プロの書き方」の基本です。

Python
from langchain_community.document_loaders import PyPDFLoader
from langchain_text_splitters import CharacterTextSplitter
from langchain_huggingface import HuggingFaceEmbeddings
from langchain_community.vectorstores import Chroma

# 1. 資料の読み込み（Ingestion）
loader = PyPDFLoader("your_document.pdf")
raw_docs = loader.load()

# 2. 意味のある単位に分割（Chunking）
# ここで「少し重ねて分割（overlap）」させるのが、文脈を逃さないコツです。
text_splitter = CharacterTextSplitter(chunk_size=500, chunk_overlap=50)
docs = text_splitter.split_documents(raw_docs)

# 3. ベクトル化して保存（Vectorization & Storage）
# E資格で学んだ「分散表現」を実際に生成し、ローカルDBに保存します。
vectorstore = Chroma.from_documents(
    documents=docs, 
    embedding=OpenAIEmbeddings(),
    persist_directory="./chroma_db"
)

from langchain_openai import ChatOpenAI
from langchain.chains import RetrievalQA

# 1. データベースのロード
# さっき保存した "chroma_db" フォルダを指定して読み込みます。
vectorstore = Chroma(
    persist_directory="./chroma_db", 
    embedding_function=OpenAIEmbeddings()
)

# 2. 回答用のAI（LLM）の準備
# ここで使うのが、Transformerベースのモデル（GPTなど）です。
embedding = HuggingFaceEmbeddings(model_name="all-MiniLM-L6-v2")

# 3. 検索と回答の「チェーン（鎖）」を作成
# 「DBから探す」→「AIに渡す」→「答える」という一連の流れを一つにまとめます。
qa_chain = RetrievalQA.from_chain_type(
    llm=llm,
    chain_type="stuff",
    retriever=vectorstore.as_retriever(search_kwargs={"k": 3})
)

# 4. 実行
query = "この資料に書いてある『安全規則』について要約して"
response = qa_chain.invoke(query)
print(response["result"])